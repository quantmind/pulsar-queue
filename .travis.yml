language: python

python:
  - 3.5.4
  - 3.6.3

services:
  - redis-server

install:
  - pip install -r requirements/dev.txt
  - pip install git+git://github.com/quantmind/pulsar.git

script:
  - make test
  - make coverage

after_script:
  - codecov
  
notifications:
  email: false
